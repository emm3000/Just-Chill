// Stability contract for classes that live OUTSIDE this module's compose compilation unit.
// The nav host and the bottom bar read :feature:* UiState classes and :core:domain values,
// neither of which carries @Stable; without this file the compose compiler treats them as
// unstable and skips nothing. A feature whose screens read state declared elsewhere carries one too.
com.emm.justchill.**
