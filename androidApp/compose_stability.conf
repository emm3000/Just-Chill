// Stability contract for classes that live OUTSIDE this module's compose compilation unit.
// The nav host and the bottom bar read :presentation state and :core:domain values, neither of
// which carries @Stable; without this file the compose compiler treats them as unstable and
// skips nothing. Mirrors ui-android/compose_stability.conf.
com.emm.justchill.**
