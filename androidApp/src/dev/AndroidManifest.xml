<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application android:networkSecurityConfig="@xml/network_security_config">

        <!-- Dev builds must not report crashes: keeps prod Crashlytics data clean and keeps the
             privacy-policy claim ("build the dev flavor for a telemetry-free app") truthful. -->
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />

        <activity android:name=".MainActivity">

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <activity
            android:name=".experiences.ShortActivity"
            android:exported="true" />

        <activity
            android:name=".me.MeActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="true" />
    </application>

</manifest>