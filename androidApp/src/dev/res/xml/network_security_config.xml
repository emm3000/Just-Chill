<?xml version="1.0" encoding="utf-8"?>
<!--
  Dev-only: permit cleartext HTTP to the local Supabase stack.
  The emulator reaches the host machine via 10.0.2.2; a physical device
  uses the LAN IP. Production (https) is unaffected — this config ships
  only in the dev flavor.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
