name: Android Action Generated APKs

on:
  push:
    branches:
      - trunk
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/trunk' }}

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: ./.github/actions/setup-android
        with:
          keystore-properties: ${{ secrets.JUST_VERONICA }}
          signing-key-base64: ${{ secrets.JUST_CHILL }}

      - name: Run tests
        run: ./gradlew :domain:test :data:testDebugUnitTest :app:testDevDebugUnitTest

  upload:
    needs: [ test ]
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: ./.github/actions/setup-android
        with:
          keystore-properties: ${{ secrets.JUST_VERONICA }}
          signing-key-base64: ${{ secrets.JUST_CHILL }}

      - name: Set current date
        run: echo "date_today=$(date +'%Y-%m-%d-%H-%M-%S')" >> $GITHUB_ENV

      - name: Build Dev Debug + Prod Release
        run: ./gradlew assembleDevDebug assembleProdRelease

      - name: Resolve APK paths
        id: paths
        run: |
          echo "devDebug=$(find app/build/outputs/apk/dev/debug -name '*.apk' | head -n1)" >> $GITHUB_OUTPUT
          echo "prodRelease=$(find app/build/outputs/apk/prod/release -name '*.apk' | head -n1)" >> $GITHUB_OUTPUT

      - name: Distribute Dev Debug to Firebase
        uses: wzieba/Firebase-Distribution-Github-Action@v1
        with:
          appId: ${{ secrets.FIREBASE_DEV_APP_ID }}
          serviceCredentialsFileContent: ${{ secrets.CREDENTIAL_FILE_CONTENT }}
          groups: EMM
          file: ${{ steps.paths.outputs.devDebug }}

      - name: Distribute Prod Release to Firebase
        uses: wzieba/Firebase-Distribution-Github-Action@v1
        with:
          appId: ${{ secrets.FIREBASE_PROD_APP_ID }}
          serviceCredentialsFileContent: ${{ secrets.CREDENTIAL_FILE_CONTENT }}
          groups: EMM
          file: ${{ steps.paths.outputs.prodRelease }}

      - name: Upload Dev Debug APK artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ env.date_today }}-dev-debug-apk
          path: app/build/outputs/apk/dev/debug/
          retention-days: 14

      - name: Upload Prod Release APK artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ env.date_today }}-prod-release-apk
          path: app/build/outputs/apk/prod/release/
          retention-days: 14
