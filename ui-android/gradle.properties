# Android-only since slice S2 (docs/swiftui/PLAN.md, docs/adr/005): the iOS app consumes
# :presentation through the JustChillKit framework, so this module's iOS targets were dropped.
# Read by KmpLibraryConventionPlugin before it registers targets.
justchill.kmp.ios=false
