// Stability contract for classes that live OUTSIDE this module's compose compilation unit.
// :presentation (extracted in slice S1) has no compose dependency, so its state classes lost
// their @Stable/@Immutable annotations; without this file the compose compiler would treat every
// cross-module UiState/model as unstable and skip nothing. :domain never had annotations but its
// values are immutable by construction (vals + value types).
com.emm.justchill.**
com.emm.domain.**
