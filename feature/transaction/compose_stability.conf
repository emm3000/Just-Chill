// Stability contract for classes that live OUTSIDE this module's compose compilation unit.
// :core:domain never had annotations but its values are immutable by construction (vals + value
// types), so without this file the compose compiler would treat every Money, TransactionId or
// CategoryType a capture or list screen takes as unstable and skip nothing.
com.emm.justchill.**
