// Stability contract for classes that live OUTSIDE this module's compose compilation unit.
// :core:domain never had @Stable annotations but its values are immutable by construction (vals +
// value types), so without this file the compose compiler would treat every Category and CategoryId
// the screens take as unstable and skip nothing.
com.emm.justchill.**
